# AZM Lending Calculator - Netlify config
# Deploy: drag this folder to Netlify, or connect via Git

[build]
  publish = "."
  command = ""

# Serve index.html for root; static assets (css, js, assets/) served as-is
