# WS2 — pricing web app (`WS2_PRICE` in `app.js`)

1. Copy `.clasp.json.example` → `.clasp.json` and set **Script ID** from this project’s Apps Script **Project settings**.
2. From repo root: `npm run clasp:pull:ws2` then edit `.gs` files here.
3. Push: `npm run clasp:push:ws2`
